#!/usr/bin/env python
from setuptools import setup

setup(
    name='pycron',
    version='1.0.1',
    description='Simple cron-like parser, which determines if current datetime matches conditions.',
    author='Asher',
    author_email='asher_as@live.com',
    url='https://github.com/asasher/pycron',
    packages=[
        'pycron',
    ],
)
